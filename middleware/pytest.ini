[pytest]
testpaths = tests
addopts = -ra
markers =
    unit: unit tests
    integration: integration tests
    contract: api contract tests
    smoke: real engine smoke tests
    gpu: gpu real engine tests
